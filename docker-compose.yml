version: "3.9"

services:
  python:
    build: .
    container_name: python_console
    working_dir: /app
    volumes:
      - .:/app
    stdin_open: true   # allows interactive input
    tty: true          # gives you a terminal